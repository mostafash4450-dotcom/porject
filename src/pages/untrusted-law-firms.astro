---
import MainLayout from '../layouts/MainLayout.astro';
import { untrustedLawFirms } from '../data/scamCompanies';

const title = "مكاتب محاماة مشبوهة - احذر التعامل معها";
const description = "قائمة تحذيرية بمكاتب المحاماة المشبوهة والاحتيالية";
---

<MainLayout title={title} description={description}>
  <div class="min-h-screen bg-gray-50 py-16 px-4">
    <div class="max-w-6xl mx-auto">
      <h1 class="text-4xl font-bold text-red-600 mb-4">مكاتب محاماة مشبوهة</h1>
      <p class="text-xl text-gray-600 mb-12">قائمة تحذيرية بمكاتب المحاماة المشبوهة والاحتيالية - احذر من التعامل معها</p>

      <div class="bg-red-50 border-l-4 border-red-600 p-6 rounded mb-12">
        <h2 class="text-lg font-bold text-red-900 mb-2">تحذير هام</h2>
        <p class="text-gray-700">
          المكاتب المدرجة في هذه القائمة معروفة بعمليات احتيال منظمة. نحن ننصح بشدة بعدم التعامل معها أو إرسال أي أموال إليها. إذا كنت قد تعاملت معها بالفعل، يرجى التواصل مع السلطات القانونية المختصة فوراً.
        </p>
      </div>

      <div class="overflow-x-auto">
        <table class="w-full bg-white rounded-lg shadow">
          <thead class="bg-red-900 text-white">
            <tr>
              <th class="px-6 py-4 text-right">اسم مكتب المحاماة</th>
              <th class="px-6 py-4 text-right">الموقع المزعوم</th>
              <th class="px-6 py-4 text-right">التحذيرات</th>
              <th class="px-6 py-4 text-right">التفاصيل</th>
            </tr>
          </thead>
          <tbody>
            {untrustedLawFirms.map((firm) => (
              <tr class="border-b hover:bg-red-50 transition-colors">
                <td class="px-6 py-4 font-semibold text-red-600">{firm.name}</td>
                <td class="px-6 py-4">{firm.location}</td>
                <td class="px-6 py-4 text-red-600 font-medium">{firm.warnings}</td>
                <td class="px-6 py-4">
                  <a href={`/law-firm/${firm.id}`} class="text-red-600 hover:text-red-700 font-medium">
                    عرض الملف الكامل
                  </a>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</MainLayout>
