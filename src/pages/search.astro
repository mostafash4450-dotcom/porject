---
import MainLayout from '../layouts/MainLayout.astro';
import { scamCompanies } from '../data/scamCompanies';
import { articles } from '../data/articles';

const searchQuery = new URL(Astro.request.url).searchParams.get('q') || '';

const results = {
  companies: scamCompanies.filter(
    (company) =>
      company.name.includes(searchQuery) ||
      company.description.includes(searchQuery) ||
      company.warnings.some((w) => w.includes(searchQuery))
  ),
  articles: articles.filter(
    (article) =>
      article.title.includes(searchQuery) ||
      article.excerpt.includes(searchQuery) ||
      article.category.includes(searchQuery)
  ),
};

const title = `نتائج البحث: ${searchQuery}`;
const description = `نتائج البحث عن "${searchQuery}" في موقع صياد الاحتيال`;
---

<MainLayout title={title} description={description}>
  <div class="min-h-screen bg-gray-50 py-16 px-4">
    <div class="max-w-6xl mx-auto">
      <div class="mb-12">
        <h1 class="text-4xl font-bold mb-2">نتائج البحث</h1>
        <p class="text-xl text-gray-600">
          البحث عن: <span class="font-bold text-blue-600">{searchQuery}</span>
        </p>
        <p class="text-gray-600 mt-2">
          وجدنا {results.companies.length + results.articles.length} نتيجة
        </p>
      </div>

      {(results.companies.length === 0 && results.articles.length === 0) && (
        <div class="bg-white rounded-lg shadow-lg p-12 text-center">
          <svg xmlns="http://www.w3.org/2000/svg" stroke-width="2" width="24" height="24" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" fill="none" viewBox="0 0 24 24" class="w-16 h-16 text-gray-400 mx-auto mb-4">
            <circle cx="11" cy="11" r="8"></circle>
            <path d="m21 21-4.3-4.3"></path>
          </svg>
          <h2 class="text-2xl font-bold text-gray-900 mb-2">لم نجد نتائج</h2>
          <p class="text-gray-600 mb-6">لا توجد نتائج تطابق بحثك. حاول استخدام كلمات مختلفة.</p>
          <a href="/" class="inline-block bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700">
            العودة للرئيسية
          </a>
        </div>
      )}

      {results.companies.length > 0 && (
        <div class="mb-12">
          <h2 class="text-3xl font-bold mb-6">شركات تداول</h2>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {results.companies.map((company) => (
              <div class="bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition-shadow">
                <div class="relative h-48 bg-gray-200">
                  <img
                    src={company.image}
                    alt={company.name}
                    class="w-full h-full object-contain p-4"
                  />
                </div>
                <div class="p-6">
                  <h3 class="text-xl font-bold text-red-600 mb-2">{company.name}</h3>
                  <p class="text-sm text-gray-500 mb-4">📍 {company.location}</p>
                  <p class="text-gray-600 mb-4">{company.description}</p>
                  <a
                    href={`/article/${company.id}`}
                    class="w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700 text-center block"
                  >
                    عرض التفاصيل
                  </a>
                </div>
              </div>
            ))}
          </div>
        </div>
      )}

      {results.articles.length > 0 && (
        <div>
          <h2 class="text-3xl font-bold mb-6">مقالات</h2>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            {results.articles.map((article) => (
              <a href={`/article/${article.slug}`} class="bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition-shadow">
                <img src={article.image} alt={article.title} class="w-full h-48 object-cover" />
                <div class="p-6">
                  <div class="flex items-center justify-between mb-3">
                    <span class="inline-block bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm">
                      {article.category}
                    </span>
                    <span class="text-sm text-gray-500">{article.date}</span>
                  </div>
                  <h3 class="text-lg font-bold text-gray-900 mb-2">{article.title}</h3>
                  <p class="text-gray-600 line-clamp-2">{article.excerpt}</p>
                </div>
              </a>
            ))}
          </div>
        </div>
      )}
    </div>
  </div>
</MainLayout>
